/// グリッドの一辺の頂点数
pub const GRID_SIZE: usize = 30;
pub const N_QUERY: usize = 1000;
pub const N_EDGE: usize = (GRID_SIZE - 1) * GRID_SIZE * 2;
pub const N_NODE: usize = GRID_SIZE * GRID_SIZE;
pub const TIME_LIMIT: f64 = 2.0;
